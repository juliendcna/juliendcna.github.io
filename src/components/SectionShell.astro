---
interface Props {
  id: string;
  heading: string;
}

const { id, heading } = Astro.props;
---

<section class="section" id={id}>
  <div class="wrap">
    <h2 class="heading" data-reveal>{heading}</h2>
    <slot />
  </div>
</section>
